# Read the Docs configuration file for Sphinx projects
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  commands:
    # 安装依赖
    - pip install -r requirements.txt
    
    # 构建中文版 (默认)
    - python -m sphinx -T -b html -d _build/doctrees -D language=zh_CN source $READTHEDOCS_OUTPUT/html
    
    # 构建英文版
    - python -m sphinx -T -b html -d _build/doctrees -D language=en source $READTHEDOCS_OUTPUT/html/en
    
    # 更多语言版本可以在这里添加
    # - python -m sphinx -T -b html -d _build/doctrees -D language=de source $READTHEDOCS_OUTPUT/html/de

sphinx:
  configuration: source/conf.py

python:
  install:
    - requirements: requirements.txt
